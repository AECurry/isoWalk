//
//  FeatureSubScreenStubs.swift
//  isoWalk
//
//  Created by AnnElaine on 3/9/26.
//
//  LOCATION: FeaturesScreen/
//
//  TEMPORARY STUBS — each will be replaced with its own
//  MVVM folder (Views / ViewModels / Components) when built.
//  Keeping them in one file until each screen is ready to build.
//

import SwiftUI

// ==========================================
// MARK: - Name & Email
// ==========================================

struct NameEmailScreenView: View {
    var body: some View {
        Text("Name & Email — Coming Soon")
            .navigationBarHidden(true)
    }
}

// ==========================================
// MARK: - Notifications
// ==========================================

struct NotificationsScreenView: View {
    var body: some View {
        Text("Notifications — Coming Soon")
            .navigationBarHidden(true)
    }
}

// ==========================================
// MARK: - Privacy
// ==========================================

struct PrivacyScreenView: View {
    var body: some View {
        Text("Privacy — Coming Soon")
            .navigationBarHidden(true)
    }
}

// ==========================================
// MARK: - Scientific Proof
// ==========================================

struct ScientificProofScreenView: View {
    var body: some View {
        Text("Scientific Proof — Coming Soon")
            .navigationBarHidden(true)
    }
}

// ==========================================
// MARK: - Submit Feedback
// ==========================================

struct SubmitFeedbackScreenView: View {
    let userName: String
    let userEmail: String

    var body: some View {
        Text("Submit Feedback — Coming Soon")
            .navigationBarHidden(true)
    }
}
